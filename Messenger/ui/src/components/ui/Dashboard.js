import {createClass} from 'react'

export const Dashboard = createClass({
    render(){
        return(
            <div>
                <h1>Dashboard</h1>
            </div>
        )
    }
})

export default Dashboard;